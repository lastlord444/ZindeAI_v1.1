/**
 * Plan Validator
 * - no consecutive same meal
 * - weekly same meal max 2
 * - same-day protein_source repeats blocked
 * - every meal tolerances + budget + alternatives present
 */
export { };

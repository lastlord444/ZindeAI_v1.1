/**
 * Seed Validator (HARD GATE)
 * - meal_class mapping
 * - meal_totals exists
 * - macro crosscheck
 * - tolerance ranges meal_type×goal_tag
 * - cost cap
 * - alternatives: 2 + kcal ±15% + protein_source different + same type/goal/class
 *
 * NOTE: implement with explicit errors; no COALESCE masking.
 */
export { };
